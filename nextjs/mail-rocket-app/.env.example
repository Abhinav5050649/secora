# Server-only: base URL of the bun/api backend. Never prefix with NEXT_PUBLIC_
# since it must not be exposed to the browser (the BFF proxy is the only thing
# that talks to it directly).
BUN_API_URL=http://localhost:3000

# Name of the httpOnly cookie that stores the backend JWT.
SESSION_COOKIE_NAME=mr_session
